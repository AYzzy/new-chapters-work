package file;

import static fresh.WordReverser.getStringBuilder;

public class workExample {
    public static StringBuilder revereWords(String words) {
        return getStringBuilder(words);
    }
}